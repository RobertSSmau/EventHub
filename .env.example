PORT=3000
NODE_ENV=development

# PostgreSQL Database (for users, events, registrations)
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASS=

# MongoDB (for chat messages and conversations)
MONGODB_URL=mongodb://localhost:27017/eventhub_chat

# JWT Authentication
JWT_SECRET=

# Redis (for rate limiting and caching)
REDIS_URL=

# Email Configuration
EMAIL_USER=
EMAIL_PASS=

# Client URL (for CORS and Socket.IO)
CLIENT_URL=http://localhost:3000